# see https://github.com/pypa/sampleproject/blob/master/setup.py for minimal setup
from setuptools import setup, find_packages

setup(
    name="aiida-profiling",
    version="0.1.0",
    packages=find_packages()
)